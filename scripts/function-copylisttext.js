function copyListText()  {
    codeList = [];
    /*
    let codeListJson = JSON.stringify(codeList);
    console.log(codeListJson);
    
    let copyText = document.getElementById('listofcodes').innerText;
    console.log(copyText);
    copyText.select();
    copyText.setSelectionRange(0, 99999);
    document.execCommand('copy');
    */
}